MEMORY {
    	CVT:    file   = %O,  start = $0,              size = $40000;
}

SEGMENTS {
	DIRENTRY:  type = ro,                 load = CVT, align      = $FE;
	FILEINFO:  type = ro,                 load = CVT, align      = $FE;
}
